# client-front

## Instal packages:

```
npm install
```

## Start development:

```
npm start
```

## Dockerize:

Build image:

```
docker build -t client-front .
```

Run image:

```
docker run -it --rm -p 80:80 client-front
```
